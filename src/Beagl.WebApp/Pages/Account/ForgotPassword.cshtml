@page "/account/forgot-password"
@model Beagl.WebApp.Pages.Account.ForgotPasswordModel
@inject Microsoft.Extensions.Localization.IStringLocalizer<Beagl.WebApp.Resources.AuthResource> L
@{
    ViewData["Title"] = L["Auth.ForgotPassword.PageTitle"];
    Layout = null;
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"]</title>
    <link rel="stylesheet" href="~/lib/bootstrap/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/lib/bootstrap-icons/font/bootstrap-icons.css" />
    <link rel="stylesheet" href="~/app.css" />
    <link rel="stylesheet" href="~/Beagl.WebApp.styles.css" />
</head>
<body>
    <section class="auth-page-shell" aria-label="@L["Auth.ForgotPassword.PageTitle"]">
        <article class="auth-card">
            <a class="app-shell__brand text-decoration-none mb-3" href="/">
                <span class="app-shell__brand-mark">
                    <img class="app-shell__brand-logo" src="~/images/beagl_logo.png" alt="@L["Auth.Login.Brand.Alt"]" />
                </span>
                <span>@L["Auth.Login.Brand.Name"]</span>
            </a>
            <p class="auth-kicker">@L["Auth.ForgotPassword.Kicker"]</p>
            <h1 class="auth-heading">@L["Auth.ForgotPassword.Title"]</h1>
            <p class="auth-copy">@L["Auth.ForgotPassword.Description"]</p>

            @if (Model.IsSubmitted)
            {
                <div class="alert alert-success" role="status" aria-live="polite">
                    <i class="bi bi-check-circle me-2" aria-hidden="true"></i>
                    @L["Auth.ForgotPassword.Success"]
                </div>
                <div class="auth-actions">
                    <a class="btn btn-primary px-4" href="/account/login">@L["Auth.ForgotPassword.Action.BackToLogin"]</a>
                </div>
            }
            else
            {
                <form method="post" class="auth-form" novalidate>
                    @Html.AntiForgeryToken()

                    @if (!ViewData.ModelState.IsValid)
                    {
                        <div class="alert alert-danger auth-error" role="alert" aria-live="assertive">
                            <div asp-validation-summary="ModelOnly"></div>
                        </div>
                    }

                    <div class="auth-field">
                        <label class="form-label" asp-for="Input.Identifier">@L["Auth.ForgotPassword.Identifier.Label"]</label>
                        <input asp-for="Input.Identifier" class="form-control" placeholder="@L["Auth.ForgotPassword.Identifier.Placeholder"]" autocomplete="username" />
                    </div>

                    <div class="auth-actions">
                        <button type="submit" class="btn btn-primary px-4">@L["Auth.ForgotPassword.Action.Submit"]</button>
                    </div>

                    <p class="auth-copy mt-2">
                        <a href="/account/login">@L["Auth.ForgotPassword.Action.BackToLogin"]</a>
                    </p>
                </form>
            }
        </article>
    </section>
</body>
</html>
